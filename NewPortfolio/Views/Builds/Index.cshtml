@model IEnumerable<NewPortfolio.Models.Build>

@{
    ViewData["Title"] = "Návody";
}
<section class="section-index-articles">
    <div class="section-hero">
<p>
    <a asp-action="Create" class="btn-use btn-succes">Vytvořit</a>
</p>
<table class="table">
    <thead>
        <tr class="articles-tr">
           
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
            <tr class="articles-tr-down">
                <td>
                   @Html.DisplayFor(modelItem => item.Name)
                </td>

            @if (User.IsInRole("admin"))
            {
                <td>
                 <a asp-action="Edit" asp-route-id="@item.Id" class="btn-use btn-edit"><i class="ri-pencil-line"></i></a>
                 <a asp-action="Details" asp-route-id="@item.Id" class="btn-use btn-detail"><i class="ri-feedback-line"></i></a>
                 <a asp-action="Delete" asp-route-id="@item.Id" class="btn-use btn-danger"><i class="ri-chat-delete-line"></i></a>
               </td>
            }
        </tr>
}
    </tbody>
</table>
<a asp-action="Index" asp-controller="BuildPosts" class="btn-use btn-edit">Zpět</a>
</div>
</section>